<Page xmlns="http://www.nativescript.org/tns.xsd" navigatingTo="navigatingTo" loaded="loaded" xmlns:app="components" actionBarHidden="true" navigatingFrom="navigatingFrom">
    <AbsoluteLayout id="pagewrapper">
        <GridLayout rows="auto, *, auto" columns="*" id="pagecontent" top="0" left="0" >
            <app:topbar />
            <!-- PAGE CONTENT -->
            
            <StackLayout row="1">
                <SearchBar id="searchbar" textFieldHintColor="#ffffff" hint="{{searchPlaceholder}}" text="{{searchText}}" />
                <Label text="{{ noSearchResultsFound }}" cssClass="search--noSearchResults" visibility="{{ noSearchResultsFound ? 'visible' : 'collapsed' }}" />
                <ListView itemTap="searchItemTap" items="{{ searchResults }}">
                    <ListView.itemTemplate>
                        <GridLayout columns="*, 48" row="auto">
                            <Label col="0" row="0" textWrap="true" cssClass="searchResultsListViewLabel">
                                <Label.formattedText>
                                    <FormattedString>
                                        <FormattedString.spans>
                                            <Span text="{{ chapter }}" fontSize="16" />
                                            <Span text="{{ ' (' + section + ')'}}" fontSize="14" foregroundColor="#879c9c" />
                                        </FormattedString.spans>
                                    </FormattedString>
                                </Label.formattedText>
                            </Label>
                            <Image col="1" row="0" cssClass="list--icon" src="{{icon}}" width="24" heigth="24" horizontalAlignment="right" />
                        </GridLayout>
                    </ListView.itemTemplate>
                </ListView>
            </StackLayout>

            <!-- /PAGE CONTENT -->
            <app:appMessage />
        </GridLayout>
        <app:sidemenu />
    </AbsoluteLayout>
</Page>
