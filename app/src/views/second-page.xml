<Page xmlns="http://www.nativescript.org/tns.xsd" loaded="pageLoaded">

<!--
    <TabView id="tabView1">
        <TabView.items>
            <TabViewItem title="Tab 1">
                <TabViewItem.view>
                    <ListView itemTap="menuItemTap" items="{{ menuItems }}">
                        <ListView.itemTemplate>
                            <Label cssClass="listviewLabel" text="{{ title }}" />
                        </ListView.itemTemplate>
                    </ListView>
                </TabViewItem.view>
            </TabViewItem>
            <TabViewItem title="Tab 2">
                <TabViewItem.view>
                    <Label text="This is Label in Tab 2" />
                </TabViewItem.view>
            </TabViewItem>
        </TabView.items>
    </TabView>
-->

    <ListView itemTap="menuItemTap" items="{{ menuItems }}">
        <ListView.itemTemplate>
            <Label cssClass="listviewLabel" text="{{ title }}" />
        </ListView.itemTemplate>
    </ListView>

</Page>

